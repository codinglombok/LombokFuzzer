# Dependencies
node_modules/

# Build output (anchored to repo root so src/ paths are never matched)
/dist/
*.tsbuildinfo

# Test coverage output
/coverage/

# Fuzzing run artifacts (anchored — must NOT match src/corpus or src/coverage)
/corpus/
/crashes/
/findings/
*.crash

# Editor / OS
.vscode/
.idea/
.DS_Store
*.log

# Env
.env
.env.local
*.bak
